import SwiftUI

@main
struct TimelineViewApp: App {
    var body: some Scene {
        Window("TimelineView", id: "main") {
            ContentView()
        }
        .defaultSize(width: 400, height: 300)
    }
}